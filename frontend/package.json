{
  "name": "permission_request",
  "version": "0.4.0",
  "description": "GeoNature permission request module",
  "license": "GPLv3",
  "scripts": {
    "format": "prettier --config .prettierrc --ignore-path .prettierignore --write '**/*.{ts,html,scss}'",
    "format:check": "prettier --check --config .prettierrc --ignore-path .prettierignore '**/*.{ts,html,scss}'"
  },
  "devDependencies": {
    "@types/mustache": "^4.2.6",
    "prettier": "~3.3"
  },
  "dependencies": {
    "mustache": "^4.2.0"
  }
}
